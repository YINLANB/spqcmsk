# 视频马赛克去除工具依赖文件
# ===========================

# 核心依赖
torch>=1.9.0
torchvision>=0.10.0
opencv-python>=4.5.0
numpy>=1.19.0

# 配置和日志
pyyaml>=5.4.0
tqdm>=4.62.0

# 图像处理
Pillow>=8.0.0
scikit-image>=0.18.0

# HTTP 下载（用于下载模型）
requests>=2.26.0

# Web 界面
gradio>=3.0.0

# 可选依赖（用于可视化和调试）
matplotlib>=3.4.0
tensorboard>=2.8.0

# 人脸修复（可选）
# gfpgan>=1.3.5
# basicsr>=1.4.2

# Windows 特定依赖
# 如果在 Windows 上安装 torch 遇到问题，可以使用以下命令：
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
